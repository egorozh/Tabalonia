<!-- PRs go to the `develop` branch. -->

## What changed

## Why

## Checklist
- [ ] `dotnet build Tabalonia.sln` passes without new warnings
- [ ] `dotnet test` passes (new behavior covered where possible)
- [ ] Both themes updated if templates changed (Custom + Fluent)
- [ ] Drag/detach behavior verified manually in Demo (if touched)
